[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "prompt-file-iterator"
version = "0.1.0"
description = "ComfyUI custom node that iterates through .txt prompt files in a directory"
requires-python = ">=3.8"
dependencies = []
